
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model order {
  id    Int     @id @default(autoincrement())
  task String  
  isComplete Boolean
}
